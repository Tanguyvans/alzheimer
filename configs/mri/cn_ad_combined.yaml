# CN vs AD classification on combined ADNI + OASIS MRI dataset
task: cn_ad
dataset: combined
output_dir: data/mri/combined_cn_ad

# MRI directories
adni_mri_dir: /home/maxglo/tanguy/ADNI-skull
oasis_mri_dir: /home/maxglo/tanguy/OASIS-skull

# Data split
seed: 42
train_ratio: 0.7
val_ratio: 0.15
test_ratio: 0.15

# Scan selection: 'first' = first scan per patient, 'all' = all scans
scan_selection: first
